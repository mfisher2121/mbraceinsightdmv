# Resend Configuration (much easier than SendGrid!)
RESEND_API_KEY=re_your-resend-api-key-here
EMAIL_FROM=noreply@mbraceinsight.com
# Note: You can start with onboarding@resend.dev, then verify your domain later

# n8n Webhook Configuration
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/dmv-audit

# Google Analytics
NEXT_PUBLIC_GA_ID=G-9SQD2P5ZJH

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual keys
# 3. Never commit .env.local to version control (it should be in .gitignore)

